[build]
    publish = "dist"
    command = "bun run build"
    node_bundler = "esbuild"

[dev]
    publish = "src"
    command = "bun run dev"
    targetPort = 5173
    node_bundler = "esbuild"

[functions]
  node_bundler = "esbuild"
